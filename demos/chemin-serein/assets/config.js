/* =========================================================================
   Chemin Serein — DEMO configuration
   -------------------------------------------------------------------------
   This is a portfolio demo. These values are placeholders and are never
   actually used to contact a real server — assets/mock-backend.js
   intercepts every request before it would ever reach the network.
   ========================================================================= */
window.HDV_CONFIG = {
  SUPABASE_URL: "https://demo.example.local",
  SUPABASE_ANON_KEY: "demo-key-not-a-real-credential"
};
